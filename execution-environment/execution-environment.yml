---
version: 3

images:
  base_image:
    name: registry.redhat.io/ansible-automation-platform-25/ee-supported-rhel9:latest

dependencies:
  galaxy: ../collections/requirements.yml
  python:
    - jmespath
  system: []

additional_build_steps:
  append_final:
    - LABEL org.opencontainers.image.source=https://github.com/mlowcher61/Infoblox
    - LABEL org.opencontainers.image.description="EE for Infoblox DNS-from-CSV role (nios_modules + universal_ddi)"
